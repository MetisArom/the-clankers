package theclankers.tripview.ui.screens.auth

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

@Composable
fun SignupScreen(navController: NavController) {
    // TODO: Implement SignUpScreen
    // appVM & LaunchedEffect, hide the navbar
    // username, email, password, firstName, lastName, likes, dislikes state
    // submit button
    // switch to login button
    // ApiService.signup(username, email, password, firstName, lastName, likes, dislikes)
}